export default function Main() {
  return (
    <main>
      <form className="add-ingredient-form">
        <input type="text" placeholder="e.g. milk" aria-label="Add an ingredient" />
        <button className="add-btn">Add ingredient</button>
      </form>
    </main>
  );
}
